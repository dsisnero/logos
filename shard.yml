name: logos
version: 0.1.0

authors:
  - Dominic Sisneros <dominic@sisneros.io>

description: |
  A fast lexer generator for Crystal, ported from Rust's Logos library.
  Create ridiculously fast lexers by defining tokens as an enum with attributes.

targets:
  logos:
    main: src/logos.cr

crystal: '>= 1.19.1'

license: MIT

dependencies:
  # Will depend on regex-syntax and regex-automata once ported

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
